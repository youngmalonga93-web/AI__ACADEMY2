#!/bin/sh
set -e

if git diff --cached --name-only | grep -E '(^|/)\.next/' >/dev/null 2>&1; then
  echo "Refusing to commit .next build output."
  exit 1
fi

cd original-file

npm run typecheck
npm run validate:imports
